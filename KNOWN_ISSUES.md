# 已知问题

## 多语言字段仍需增强

阿拉伯语、英语和其他多语言标签已支持通用字段定义、空间绑定、单位校验和值类型校验，但仍需要更多国家、语言和标签版式样本验证。

## 首次新图纸解析可能较慢

首次上传或解析新 PDF 图纸时需要执行图纸 OCR 和字段模板生成，耗时可能明显高于现场标签检测。试运行前建议先完成图纸入库和字段模板确认。

## 图片质量依赖拍摄规范

反光、模糊、倾斜、遮挡和标签占比不足会影响 OCR 结果。现场试运行需要制定拍照距离、角度、光照和背景要求。

## 真实图纸模板需要人工确认

系统可以自动生成图纸字段模板，但真实业务中模板必须由工程或质量人员确认后再用于现场检测。

## 二维码匹配关系仍需人工确认

现场检测端已支持人工选择/上传正确图纸并绑定二维码，但这属于 MVP 试运行增强，不是正式权限审批流程。疑似图纸与标签不一致时应进入 NEED_REVIEW。

## 本地 JSONL 不是正式数据库

当前检测记录适合 MVP 试运行和演示，不适合作为正式生产数据库。正式上线需要使用数据库、备份策略和权限控制。

## 权限和审批未做

当前版本未完成正式用户权限、角色审批、审计追踪和电子签核流程。

## 异常复核闭环未完成

当前版本可以提示 FAIL 和 NEED_REVIEW，但尚未提供正式异常审批、复判、关闭和追溯流程。

## MES / PLM 未接入

当前版本未与 MES、PLM、ERP 或质量系统正式集成。产品主数据、图纸版本和检测记录仍以本地项目数据为主。

## 真实企业样本未提交 GitHub

真实企业图纸、标签、records、debug 输出和本地 master_data 已被忽略，不应随代码仓库公开提交。

## 云端写入能力需部署后验证

Streamlit Cloud 或其他云端环境的 `records/evidence` 写入能力需要在部署后再次验证。

## 不适合替代人工全检

当前版本是 MVP 试运行候选版，不是正式生产放行系统，不能承诺完全替代人工检测。
