# 图纸驱动标签检测 MVP 试用说明

## 进入系统

本地试用时运行 Streamlit 入口：

```bash
python -m streamlit run app_quality_terminal.py
```

部署试用时打开项目对应的 Streamlit Cloud 页面。

## 演示模式怎么用

1. 在左侧页面选择“演示模式”。
2. 选择演示样例。
3. 点击预览图纸字段模板，确认系统加载了图纸标准字段。
4. 上传或拍摄标签图片。
5. 点击开始检测。
6. 查看总览、字段结果表和折叠调试信息。
7. 需要留档时保存演示检测记录或导出报告。

## 现场检测端怎么用

1. 在左侧页面选择“现场检测端”。
2. 扫描二维码、上传二维码图片，或手动输入二维码内容。
3. 查看二维码匹配确认区，确认系统匹配到的产品和图纸是否正确。
4. 如果匹配不确定或不正确，可以手动选择已有图纸，或现场上传正确 PDF 图纸。
5. 如需沉淀关系，可点击“将当前二维码绑定到此图纸”。
6. 加载当前实际使用图纸的字段模板。
7. 上传或拍摄当前实物标签。
8. 查看图片质量提示。
9. 开始 OCR 识别和字段级比对。
10. 查看字段比对结论、模板状态和最终建议。
11. 保存检测记录。

系统会优先根据当前图纸模板中的字段，在标签 OCR 结果里按字段名、位置、单位和值类型寻找对应值；不确定时会显示 NEED_REVIEW。
如果标签型号、标准编号、品牌或产地与当前图纸明显不一致，系统会提示疑似图纸匹配错误，并要求人工确认图纸匹配关系。

## 字段模板确认怎么用

1. 在左侧页面选择“字段模板确认”。
2. 选择需要确认的图纸。
3. 检查字段名、原始字段名、标准值、字段 key 和是否参与检测。
4. 图纸管理字段、备注、编码等不应默认参与现场标签检测。
5. 确认无误后保存模板状态。

## 图片质量提示怎么看

- PASS：图片质量可用于试运行检测。
- WARNING：图片存在风险，建议人工复核。
- FAIL：建议重拍。若继续检测，结果仅适合试运行或调试参考。

图片质量风险不会直接等同于标签字段错误。

## 检测结果怎么看

页面会显示三类信息：

- 字段比对结论：字段值是否与图纸模板一致。
- 模板状态：图纸字段模板是否已人工确认。
- 最终建议：结合字段结果、模板状态和图片质量给出的使用建议。

字段级表格会显示：

- 中文字段名
- 原始字段名
- 图纸标准值
- 标签识别值
- 检测状态
- 判定原因

## PASS / FAIL / NEED_REVIEW

- PASS：图纸标准值与标签识别值明确一致。
- FAIL：图纸标准值与标签识别值明确不一致。
- NEED_REVIEW：OCR 不完整、字段无法稳定定位、字段语义或模板状态需要人工确认。

## 什么时候需要人工复核

- 出现 NEED_REVIEW 字段。
- 图片质量为 WARNING 或 FAIL。
- 图纸模板尚未人工确认。
- 二维码匹配图纸不确定，或人工发现系统匹配图纸不正确。
- 标签反光、模糊、倾斜或遮挡。
- 多语言字段识别结果不完整。
- 系统提示字段候选值存在冲突。
- 系统提示字段值疑似错位或字段名被识别成字段值。

当前版本不建议直接替代人工全检。
